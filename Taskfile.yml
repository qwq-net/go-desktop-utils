version: "3"

vars:
  OUTPUT: desktop-widget.exe
  LDFLAGS: -H windowsgui -s -w

tasks:
  build:
    desc: Build Windows GUI binary
    cmds:
      - GOOS=windows GOARCH=amd64 go build -ldflags="{{.LDFLAGS}}" -o {{.OUTPUT}} ./cmd/desktop-widget
    sources:
      - cmd/**/*.go
      - internal/**/*.go
      - go.mod
      - go.sum
    generates:
      - "{{.OUTPUT}}"

  tidy:
    desc: Run go mod tidy
    cmds:
      - go mod tidy

  dist:
    desc: "Create distribution zip (usage: task dist VERSION=v1.0.0)"
    deps: [build]
    vars:
      DIST_DIR: dist/desktop-widget
      ZIP_NAME: 'desktop-widget{{if .VERSION}}-{{.VERSION}}{{end}}.zip'
    cmds:
      - rm -rf dist
      - mkdir -p {{.DIST_DIR}}
      - cp {{.OUTPUT}} {{.DIST_DIR}}/
      - cp configs/config.json {{.DIST_DIR}}/
      - cd dist && zip -r {{.ZIP_NAME}} desktop-widget/

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -rf {{.OUTPUT}} dist

  default:
    desc: Build (default task)
    deps: [build]
